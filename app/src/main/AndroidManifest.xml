<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.breno.pokemobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuPrincipalActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".SelecionaTreinadorActivity" />
        <activity android:name=".CriaTreinadorActivity" />
        <activity android:name=".LojaActivity" />
        <activity android:name=".MochilaActivity" />
        <activity android:name=".PokedexActivity" />
        <activity android:name=".DetalhesPokemonActivity" />
        <activity android:name=".SelecionaPokemonActivity" />
        <activity android:name=".PokemonsActivity"></activity>
    </application>

</manifest>